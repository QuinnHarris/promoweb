%table
  %thead
    %td #
    %td Created
    %td Last Modified
  %tbody
    - for order in @order.customer.orders
      %tr
        %td= link_to order.id, status_order_path(order)
        %td= format_time(order.created_at, false)
        %td= format_time(order.updated_at, false)
