%h1 Users

= link_to "Add User", :action => :new

%table
  %thead
    %tr
      %td Action
      %td Login
      %td Name
      %td Email
  %tbody
    - for user in @users
      %tr
        %td
          = link_to 'Edit', :action => :edit, :id => user
          ,
          = link_to 'Phone', admin_user_phones_path(user)
        %td= user.login
        %td= user.name
        %td= user.email

