- customer.fields_for :phone_numbers, phone_number do |phone|
  .phone_number
    - unless @static
      = phone.select(:name, PhoneNumber.types)
      = phone.text_field :number_string, :size => 19
      = remove_link(phone)
    - else
      #{phone.object.name}: #{phone.object.number_string}
